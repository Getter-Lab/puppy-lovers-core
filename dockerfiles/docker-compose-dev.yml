version: '3.9'
services:
  pg_database:
    image: postgres
    container_name: 'template_pg'
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: example
      POSTGRES_DB: example

networks:
  default:
    driver: bridge
